[build]
rustflags = ["-C", "target-cpu=native"]

[alias]
bindgen = "run -p bitviewd --bin bitview-bindgen --features bindgen"
dev = "run -p bitviewd --bin bitview-bindgen --features bindgen -- --run"
prod = "run --release -p bitviewd"
