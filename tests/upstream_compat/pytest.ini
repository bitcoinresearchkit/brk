[pytest]
testpaths = .
python_files = test_*.py
addopts = -ra
markers =
    live: requires the target HTTP server and upstream reference APIs
    regtest: successful broadcasts; requires disposable regtest and funded fixtures
